export interface IProject {
  created_at: string;
  description: string;
  environment: string;
  id: string;
  is_default: boolean;
  name: string;
  owner_id: number;
  owner_uuid: string;
  purpose: string;
  updated_at: string;
}
